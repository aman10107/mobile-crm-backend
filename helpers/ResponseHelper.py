from rest_framework.response import Response
from rest_framework import status

# class ResponseHelper():
#     class responseType():
#         serializerError = "serializer_error"
#     def getResponnse(response, responseType=None, reponseStatus=None):
#         if responseType == responseType.serializerError:
#             return Response()

